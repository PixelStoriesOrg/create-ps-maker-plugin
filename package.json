{
  "name": "ps-maker-plugin-cli",
  "version": "0.1.0",
  "description": "Create and build Pixel Stories Maker plugins",
  "bin": {
    "create-ps-maker-plugin": "dist/index.js",
    "ps-maker-plugin": "dist/index.js"
  },
  "type": "module",
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "esbuild": "^0.24.0",
    "picocolors": "^1.1.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/prompts": "^2.4.9",
    "tsup": "^8.3.0",
    "typescript": "^5.7.0"
  }
}

